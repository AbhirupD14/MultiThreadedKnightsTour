# MultiThreadedKnightsTour
A multi threaded solution to the classic Knights Tour problem written in C. -D NO_PARALLEL can be used during compilation to turn on non-parallel processing. Parallel processing is the default mode.
